# 项目架构说明

## UI 系统架构（2026-01-02 完全重构）

本项目采用 VSCode 风格的现代化 UI 架构，提供专业的视频编辑开发体验。

### 核心技术栈

- **框架**: Next.js 14 + React 18 + TypeScript
- **布局**: Allotment（VSCode 官方分割面板库）
- **代码编辑**: Monaco Editor
- **视频引擎**: Remotion
- **状态管理**: Zustand
- **样式**: Tailwind CSS
- **拖拽**: @dnd-kit

### 布局结构

```
┌─────────────────────────────────────────────────────┐
│                  VideoEditor (Root)                  │
├───┬─────────────────────────────────────────────────┤
│ A │              Allotment (水平分割)                │
│ c │  ┌──────┬────────────────────────────────────┐  │
│ t │  │      │                                    │  │
│ i │  │ Side │         EditorArea                 │  │
│ v │  │ bar  │  ┌────────────────────────────┐   │  │
│ i │  │      │  │  标签栏 (可拖动)          │   │  │
│ t │  │ (可  │  ├────────────────────────────┤   │  │
│ y │  │  选) │  │ Allotment (垂直分割)      │   │  │
│   │  │      │  │  ┌──────────────────────┐ │   │  │
│ B │  │      │  │  │   CodeEditor        │ │   │  │
│ a │  │      │  │  ├──────────────────────┤ │   │  │
│ r │  │      │  │  │   VideoPreview      │ │   │  │
│   │  │      │  │  │   ├──────────────┐  │ │   │  │
│   │  │      │  │  │   │ Player       │  │ │   │  │
│   │  │      │  │  │   ├──────────────┤  │ │   │  │
│   │  │      │  │  │   │ Timeline     │  │ │   │  │
│   │  │      │  │  │   └──────────────┘  │ │   │  │
│   │  │      │  │  └──────────────────────┘ │   │  │
│   │  │      │  └────────────────────────────┘   │  │
│   │  └──────┴────────────────────────────────────┘  │
├───┴─────────────────────────────────────────────────┤
│                    StatusBar                         │
└─────────────────────────────────────────────────────┘
```

### 核心组件说明

#### 1. VideoEditor (主容器)
- **职责**: 整体布局管理、全局状态协调
- **特性**: 
  - Flex 布局，高度 100vh
  - 管理 Activity Bar、侧边栏、主内容区、状态栏
  - 使用 Allotment 实现可调整大小的面板

#### 2. ActivityBar (活动栏)
- **职责**: 提供快捷导航入口
- **特性**:
  - 固定宽度 48px
  - 包含 AI助手、资源、模板、代码、预览、设置
  - VSCode 风格的高亮和交互

#### 3. Sidebar (侧边栏)
- **职责**: 显示辅助工具面板
- **特性**:
  - 可调整宽度 (200-600px)
  - 根据 Activity 动态显示内容
  - 可隐藏

#### 4. EditorArea (编辑区)
- **职责**: 管理代码编辑器和视频预览
- **特性**:
  - 支持标签页切换
  - 支持标签拖动排序 (@dnd-kit)
  - 自动垂直分割布局（编辑器在上，预览在下）
  - 懒加载组件优化性能

#### 5. CodeEditor (代码编辑器)
- **职责**: 提供代码编写功能
- **特性**:
  - Monaco Editor 集成
  - TypeScript 语法高亮
  - 实时编译（防抖 1s）

#### 6. VideoPreview (视频预览)
- **职责**: 视频预览和控制
- **特性**:
  - Remotion Player 集成
  - 实时预览编译后的视频
  - 垂直分割：播放器 + 时间轴
  - 导出功能（MP4, WebM, GIF, 图片序列）
  - 视频设置（分辨率、FPS、时长）

#### 7. Timeline (时间轴)
- **职责**: 视频时间轴控制
- **特性**:
  - 时间刻度显示
  - 播放控制（播放/暂停、前进/后退）
  - 缩放功能
  - 拖动调整播放位置

#### 8. StatusBar (状态栏)
- **职责**: 显示全局状态信息
- **特性**:
  - 编译状态
  - 视频参数（FPS、帧数、分辨率）

### 状态管理

#### useCodeStore
- 代码内容 (code)
- 视频配置 (videoConfig)
- 持久化到 LocalForage

#### useEditorStore
- 活动栏状态 (activeActivity)
- 标签页状态 (activeTabs)
- 侧边栏宽度 (sidebarWidth)
- 编译状态 (compiling, compilationError)

#### useTimelineStore
- 当前时间 (currentTime)
- 播放状态 (isPlaying)
- 缩放级别 (zoom)
- 时长信息 (duration, fps)

### 关键特性

#### 1. 可调整大小的面板
- 使用 Allotment 库实现 VSCode 风格的分割面板
- 支持拖动调整大小
- 支持最小/最大尺寸限制
- 支持吸附功能

#### 2. 响应式布局
- 所有容器使用 Flexbox
- 正确处理 `min-h-0` 和 `min-w-0` 防止溢出
- 使用 `overflow-hidden` 确保内容不溢出

#### 3. 错误隔离
- 每个组件都包裹在 ErrorBoundary 中
- 编译错误不影响 UI 正常使用
- 非阻塞式错误提示

#### 4. 性能优化
- 懒加载组件 (dynamic import)
- 防抖编译 (1s)
- Suspense 加载状态
- 避免不必要的重渲染

### 样式规范

#### VSCode 主题色
- 背景色: `#1e1e1e`
- 次要背景: `#252526`, `#2d2d30`
- 边框色: `#3e3e42`
- 文字色: `#cccccc`
- 次要文字: `#969696`
- 强调色: `#007acc` (蓝色)
- 悬停色: `#37373d`

#### 布局原则
1. 使用 Flexbox 作为主要布局方式
2. 固定尺寸的元素使用 `flex-shrink-0`
3. 弹性元素使用 `flex-1` + `min-h-0`/`min-w-0`
4. 避免使用绝对定位（除了悬浮层）
5. 所有可交互元素设置 `cursor: pointer`

### 重构要点（对比旧版本）

#### 1. 简化了 CSS
- 移除了冗余的 Allotment 样式覆盖
- 只保留必要的 VSCode 风格定制
- 减少 `!important` 的使用

#### 2. 简化了组件结构
- 移除不必要的容器嵌套
- 统一使用 Allotment 管理布局
- 移除了 Panel 组件（底部面板）

#### 3. 改进了状态管理
- 移除了不必要的状态
- 优化了状态更新逻辑
- 避免了循环依赖

#### 4. 改进了错误处理
- 统一的错误边界
- 非阻塞式错误显示
- 更清晰的错误信息

### 开发指南

#### 添加新的 Activity
1. 在 `ActivityBar.tsx` 中添加活动定义
2. 在 `Sidebar.tsx` 中添加对应的内容
3. 在 `VideoEditor.tsx` 中处理活动切换逻辑

#### 添加新的标签页
1. 在 `EditorArea.tsx` 中添加标签定义
2. 创建对应的组件
3. 使用 `dynamic` 导入实现懒加载

#### 调整布局
1. 修改 `Allotment` 的 `minSize`、`maxSize`、`preferredSize`
2. 使用 `proportionalLayout={false}` 实现像素级控制
3. 使用 `onChange` 回调同步状态

### 已知问题

1. ~~ExportDialog 的 props 类型不匹配~~ (已修复)
2. ~~按钮点击事件有时不响应~~ (已修复)
3. ~~组件大小调整后显示不全~~ (已修复)

### 后续优化方向

1. 添加键盘快捷键支持
2. 添加撤销/重做功能
3. 添加代码自动补全
4. 添加实时协作功能
5. 优化大文件编辑性能

